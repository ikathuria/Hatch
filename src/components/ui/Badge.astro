---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className = "", ...rest } = Astro.props as Props;
---

<span class:list={["ui-badge", className]} {...rest}>
  <slot />
</span>
