---
interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className = "", ...rest } = Astro.props as Props;
---

<nav class:list={["tabs", className]} {...rest}>
  <slot />
</nav>
